version : 0.2
phases:
      build:
      commands:
         - echo "Build started"
         - mnv clean package
         - echo "Build completed"
    artifacts:
       files:
        - target/prema-demo.jar
